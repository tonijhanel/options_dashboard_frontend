import PageHeader from '../components/PageHeader';
import HedgeCard from '../components/HedgeCard';

export default function HedgePage() {
  return (
    <div>
      <PageHeader title="Hedge" />
      <HedgeCard />
    </div>
  );
}
